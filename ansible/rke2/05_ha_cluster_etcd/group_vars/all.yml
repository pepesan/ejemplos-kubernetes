lxd_network: "lxdbr0"
lxd_image: "k8s-template"
rke2_version: "v1.36.3+rke2r1"
rke2_cni: "canal"
rke2_token: "secret-rke2-ha-token-2026"

# Control-plane VIP advertised by kube-vip (ARP + leader election) on the
# server nodes. Also used as the join endpoint (port 9345) for additional
# servers and workers, and as the kubeconfig server address (port 6443).
rke2_vip: "10.207.154.60"
kube_vip_version: "v1.2.2"
