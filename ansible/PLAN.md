# 🗺️ Plan de Ruta

Índice del plan de ruta y estado de este repositorio. El contenido detallado vive en cada subdirectorio:

- [`base/PLAN.md`](base/PLAN.md) — roadmap, estado de validación de cada laboratorio, lecciones
  aprendidas y "cosas a comprobar" de los 14 laboratorios numerados.
- [`roles/PLAN.md`](roles/PLAN.md) — backlog y estado de los roles de Ansible reutilizables
  (`lxd_host_bootstrap`, `lxd_machine_provision`, y los planeados).

## Pendiente, a nivel de todo el repositorio

- Evaluar `ansible-lint` (config + primera pasada de diagnóstico) sobre todo el árbol (`base/` y
  `roles/`) — no decidido cuándo abordarlo.
