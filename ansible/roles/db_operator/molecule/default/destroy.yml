---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Delete every Kubernetes test VM created by this scenario
      ansible.builtin.command: "lxc delete --force {{ item }}"
      loop: "{{ groups['k8s_nodes'] }}"
      changed_when: true
      failed_when: false
