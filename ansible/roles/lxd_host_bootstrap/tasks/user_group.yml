---
- name: Add the host user to the lxd/incus-admin group (so lxc commands work without sudo)
  ansible.builtin.user:
    name: "{{ lxd_host_user }}"
    groups: "{{ lxd_group_name }}"
    append: true
  register: lxd_user_group_update
